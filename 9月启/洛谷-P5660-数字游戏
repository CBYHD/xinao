#include <iostream>
#include <cstring>
using namespace std;
char a[1000];
int sum = 0;
int main()
{
    cin.getline(a, 1000);
    for (int i = 1; i <= strlen(a); i++)
    {
        if (a[i] == '1')
        {
            sum++;
        }
    }
    cout << sum;
    return 0;
}