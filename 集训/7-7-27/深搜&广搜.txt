//深搜
void dfs(int x){    
    //加入剪枝

    //枚举每一个可能搜素到的位置
    for(1->n){
        //该位置是否有效
        if(){
            //标记该位置
            //记录
            dfs(); //继续搜索
            //恢复现场 去标记
        }
    }
}
//广搜
void bfs(){
    //先将起点入队
    while(head < tail){
        //枚举每一个可能跟队头元素相连的位置
        for(1 -> n){
            //判断是否是有效位置
            if(){
                //标记 
                //出队操作
            }
        }
        head++;//出队
    }
}